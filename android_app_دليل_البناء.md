# دليل بناء وتشغيل تطبيق الأندرويد

المرفق `cash_topup_forwarder_app.zip` فيه **مشروع Android Studio كامل** بلغة Kotlin.
مش ملف APK جاهز — محتاج تبنيه مرة واحدة على جهازك (الخطوات تحت سهلة ومتاخدش
وقت طويل، وبعدها هيبقى عندك APK تقدر تركبه على أي عدد تلفونات تحب).

## ليه مش بعتلك APK جاهز؟
عشان بناء تطبيق أندرويد محتاج بيئة Android SDK ومكتبات Google الرسمية اللي
مش متاحة في بيئة التنفيذ بتاعتي. الحل الأضمن إني أديك الكود كامل وجاهز
تبنيه بضغطة زرار من Android Studio (مجاني ومتاح للجميع).

---

## الخطوة 1: تثبيت Android Studio
حمّل وثبّت Android Studio من الموقع الرسمي: https://developer.android.com/studio
(البرنامج مجاني، والتثبيت بيجيب SDK المطلوب تلقائي أول مرة تفتحه).

## الخطوة 2: فتح المشروع
1. فك ضغط `cash_topup_forwarder_app.zip`.
2. من Android Studio: **File → Open** واختار مجلد `android_app`.
3. استنى أول مرة لحد ما Gradle يخلص التحميل والمزامنة (Sync) — محتاج نت.

## الخطوة 3: بناء الـ APK
من القائمة العلوية:
**Build → Build Bundle(s) / APK(s) → Build APK(s)**

بعد ما يخلص هتلاقي إشعار "APK(s) generated successfully" فيه رابط
**locate** يوديك على مكان الملف (بيكون تقريباً في:
`android_app/app/build/outputs/apk/debug/app-debug.apk`).

> ده APK للتجربة (debug) وكفاية تماماً للاستخدام الداخلي على تلفون
> الاستقبال بتاعك. لو حبيت تنشره رسمي على متجر لازم تعمل "Signed APK"
> من نفس القائمة (Generate Signed Bundle / APK)، لكن للاستخدام الشخصي
> مش لازم.

## الخطوة 4: تركيب التطبيق على تلفون الاستقبال
1. انسخ ملف `app-debug.apk` على التلفون (عن طريق كابل أو رابط تحميل مباشر).
2. افتح الملف من التلفون وثبّته (هيطلب منك تسمح بـ "التثبيت من مصادر غير
   معروفة" — سماح عادي لأي APK مش من المتجر).

## الخطوة 5: إعداد التطبيق بعد التثبيت
1. افتح التطبيق.
2. اضغط **"طلب صلاحية قراءة الرسائل"** ووافق على الأذونات.
3. اكتب في الحقلين:
   - **رابط الـ API:** `https://yourdomain.com/api/receive_sms.php`
   - **المفتاح السري:** نفس القيمة اللي حطيتها في `config_sms.php`
4. اضغط **"حفظ الإعدادات"**.
5. اضغط **"استثناء التطبيق من توفير البطارية"** عشان أندرويد ميوقفش
   التطبيق في الخلفية.
6. اضغط **"تشغيل خدمة المراقبة"** — هتظهر إشعار ثابت يقولك إن النظام شغال.

## الخطوة 6: الاختبار
- حوّل مبلغ تجريبي من رقم موزع حقيقي على الشريحة اللي جوه التلفون.
- في نفس شاشة التطبيق اضغط **"تحديث السجل"** وهتشوف نتيجة إرسال الرسالة
  للسيرفر (نجاح / خطأ / مش متطابقة).
- تأكد كمان من جدول `cash_transactions` في قاعدة البيانات إن الحالة
  `matched` والرصيد اتضاف فعلاً في جدول `distributor`.

---

## ملاحظات مهمة
- التطبيق بيتعامل مع أكتر من شريحة/شركة في نفس الوقت طالما كلهم على
  نفس التلفون (بيحاول يخمن اسم الخدمة من الرسالة، والسيرفر أصلاً بيحلل
  النص بنفسه بغض النظر عن التخمين ده).
- لو التلفون فيه أكتر من شريحة (Dual SIM)، التطبيق بيستقبل رسائل الشريحتين
  عادي من غير أي إعداد إضافي.
- لو حصل وقفل التلفون فجأة وفتح تاني، فيه `BootReceiver` بيشغل الخدمة
  تلقائي طالما الإعدادات محفوظة.
- ابعتلي أي رسالة خطأ ظهرت في السجل جوه التطبيق أو في عمود `raw_message`
  بقاعدة البيانات، وهظبطلك التحليل (regex) لو الصيغة مختلفة عن المتوقع.
